// import axios from "axios";
// import { IEvent } from "../IEventInterface";

// const useApi = () => {
//   const baseApi = import.meta.env.VITE_BASE_API as string;
//   const addNewEvent = async (data: IEvent) => {
//     try {
//       const response = await axios.post(`${baseApi}/events`, data);
//       return response.data;
//     } catch (error) {
//       throw error;
//     }
//   };
// };

// export default useApi;
